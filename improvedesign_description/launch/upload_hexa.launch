<launch>

  <!-- Loading PR2 description on parameter server -->
  <param name="robot_description" command="$(find xacro)/xacro $(find improvedesign_description)/urdf/improvedesign.urdf.xacro" />

  <!-- Spawn a robot into Gazebo -->
<!--  <node name="spawn_urdf" pkg="gazebo_ros" type="spawn_model" args="-param robot_description -urdf -model improvedesign" />
-->
</launch>
